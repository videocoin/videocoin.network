kind: pipeline
type: kubernetes
name: dev.videocoin.network

clone:
  git:
    image: plugins/git
    recursive: true
    submodule_override:
      ui-kit: https://github.com/videocoin/cloud-ui-kit.git

steps:
  - name: build
    image: plugins/docker
    settings:
      username:
        from_secret: registry_username
      password:
        from_secret: registry_password
      registry: registry.videocoin.net
      repo: registry.videocoin.net/cloud/videocoin-network
      build_args:
        - GA_TRACKING_ID=
        - AW_TRACKING_ID=
      tags:
        - ${DRONE_COMMIT_SHA}
  - name: deploy
    image: devth/helm:v3.1.1
    environment:
      KUBE_CONFIG:
        from_secret: dev_kube_config
    commands:
      - echo $KUBE_CONFIG | base64 -d > kube.config
      - helm --kubeconfig=kube.config upgrade -i --wait --set image.tag=$DRONE_COMMIT_SHA -n console videocoin-network ./deploy/helm

trigger:
  event:
    - push
    - pull_request
  branch:
    - drone
    - infra
